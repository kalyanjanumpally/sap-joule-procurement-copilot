# Anthropic (Path B — local dev / testing)
ANTHROPIC_API_KEY=sk-ant-...

# Ollama (fully local; no key needed unless remote)
OLLAMA_BASE_URL=http://localhost:11434

# Groq (fast cloud inference, generous free tier)
GROQ_API_KEY=gsk_...

# Generic OpenAI-compatible endpoint (Together, Fireworks, DeepSeek, LM Studio...)
OPENAI_API_KEY=sk-...
OPENAI_BASE_URL=https://api.openai.com/v1

# SAP Generative AI Hub (Path A — BTP deployment)
AICORE_DEPLOYMENT_ID=
AICORE_RESOURCE_GROUP=default
